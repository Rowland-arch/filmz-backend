PORT=5000
JWT_SECRET=replace-with-a-long-random-secret
ADMIN_EMAIL=admin@example.com
# Set this only in your local .env or hosting provider secrets; never commit it.
ADMIN_PASSWORD=replace-with-your-admin-password
